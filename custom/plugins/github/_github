#compdef github
#autoload

# in order to make this work, you will need to have the github gem installed
# http://github.com/defunkt/github-gem

# github zsh completion, based on homebrew completion

local -a _1st_arguments
_1st_arguments=(
    'empty_gh:create a new repo from scratch'
    'new_gh:setup GitHub repo settings'
    'exist_gh:when you already has git repo, but add this to GitHub'
    'fork_gh:fork a GitHub repo'
    'upstream_merge_gh:fetch and merge upstream'
    'submodule_init_update_gh:init, update, foreach to update submodules'
    'pages_gh:checkout GitHub Pages branch'
)

local expl
local -a pkgs installed_pkgs

_arguments \
  '*:: :->subcmds' && return 0

if (( CURRENT == 1 )); then
  _describe -t commands "github subcommand" _1st_arguments
  return
fi
